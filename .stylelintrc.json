{
  "extends": ["stylelint-config-standard-scss"],
  "ignoreFiles": ["components/ui/**/*"],
  "rules": {
    "scss/at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["custom-variant", "theme", "tailwind", "import", "layer", "apply", "config"]
      }
    ],
    "at-rule-no-unknown": null
  }
}
